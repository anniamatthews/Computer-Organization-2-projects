#include <iostream>
#include <fstream>
using namespace std; 

int main(){
    // generate binary file "file.bin"
    ofstream file("file.bin",ios::binary | ios::out);

    // define buffer of 100 bytes
    char buffer[100];
    
     





    return 0; 
}